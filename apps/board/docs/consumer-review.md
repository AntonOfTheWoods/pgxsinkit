# Consumer review log (docs dogfooding)

Running log of every moment building `apps/board` required reading `@pgxsinkit/*`
**source** because the docs/`llms.txt` did not answer the question — i.e. a
documentation gap a real external consumer would also hit. See
[ADR-0006](./adr/0006-docs-dogfooding-gate.md) for the process. Fixes land in the
toolkit docs (Starlight content + source JSDoc); `llms.txt` regenerates from them.

Status: `open` → `resolved <commit>` (doc/JSDoc updated) · `n/a-internal` (consumer
never needs it) · `ergonomics` (a real API gap, fixed upstream).

## Phase 1 — board-schema (registry, RLS, conflict policy, consistency groups)

| #   | What I needed                                                                                                                                                                                                                                                                                                             | Where I looked                                               | In the docs?                                                                                                                                                    | Resolution |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 1   | **RLS read/write two-subject split**: write-path policies key on `auth.uid()` (Postgres-with-JWT), but the read-path `customWhere` must key on the literal `claims.sub` because **Electric** runs the `where`, not Postgres. Getting this wrong silently breaks security.                                                 | concepts/read-path, concepts/write-path, electric-subqueries | partial — subquery `where` is covered, but not that the subject source differs between the two paths                                                            | open       |
| 2   | **Local schema emits no FK constraints** (parity boundary), so a child table grouped with its parent in a consistency group needs **no** `deferrableConstraints`; `deferrableConstraints` is a _write-path_ server concern only.                                                                                          | concepts/local-schema-ddl-parity                             | partial — parity boundary lists FK as "not yet local", but the consistency-group ↔ FK interaction and "when do I need deferrableConstraints" is not spelled out | open       |
| 3   | **`conflictPolicy` is a required hard-error** for every writable table (registry validation throws). Discovered from source/ADR, not a getting-started rule.                                                                                                                                                              | start/getting-started, api/contracts                         | unclear — need to confirm a writable-table example shows it as required                                                                                         | open       |
| 4   | **The membership RLS builder gates writes to owner-or-manager.** For collaborative any-member writes you compose your own policies from the exported primitives (`escapeSqlLiteral`, `pgPolicy`, the predicate-text builders). The "builders cover these shapes; here's how to hand-author beyond them" story is missing. | api/contracts (supabase-rls), concepts                       | no                                                                                                                                                              | open       |
| 5   | **The server is a runtime-portable `fetch` handler** (`createSyncServer().fetch`, or `registerMutationRoute`/`proxyElectricShapeRequest` on your own Hono app), so it runs on Deno / Supabase Edge Functions, not only Bun.                                                                                               | api/server, concepts/write-path, getting-started             | no — docs imply a Bun server                                                                                                                                    | open       |
| 6   | **Managed fields + Server version**: every writable table must declare a `nowMicroseconds`-on-update managed field (the Server version, conventionally `updated_at_us`); `authUid` stamps owner/author columns server-side and the API rejects them in the client payload.                                                | start/getting-started, api/contracts                         | unclear — confirm getting-started shows the managed-field block for a writable table                                                                            | open       |

> Phase 1 gate: triage these → update Starlight content + source JSDoc → `docs:build` → verify `llms.txt`, then mark resolved.
